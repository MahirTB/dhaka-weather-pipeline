---
# Welcome to your prefect.yaml file! You can use this file for
# storing and managing configuration for deploying your flows.
# We recommend committing this file to source control along
# with your flow code.

# Generic metadata about this project
name: Automated_Weather_Pipeline
prefect-version: 3.6.24

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project
# is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning
# this project in remote locations
pull:
- prefect.deployments.steps.git_clone:
    repository: https://github.com/MahirTB/dhaka-weather-pipeline.git
    branch: main

# the deployments section allows you to provide configuration
# for deploying flows
deployments:
- name: dhaka-weather-every-2-hours
  version: null
  tags: []
  concurrency_limit: null
  description: Prefect flow wrapper around the Dhaka weather ETL pipeline.
  entrypoint: prefect_flow.py:dhaka_weather_prefect_flow
  parameters: {}
  work_pool:
    name: dhaka_weather_pool
    work_queue_name: null
    job_variables:
      pip_packages:
      - boto3
      - pandas
      - plotly
      - python-dotenv
      - pyarrow
      - requests
      - s3fs
  schedules:
  - cron: 0 */2 * * *
    timezone: null
    day_or: true
